apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-postgres-pvc
  namespace: affine
  labels:
    app: affine
    component: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: nfs-client-re # Explicitly set to your NFS storage class

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-storage-pvc
  namespace: affine
  labels:
    app: affine
    component: server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: nfs-client-re # Explicitly set to your NFS storage class

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-config-pvc
  namespace: affine
  labels:
    app: affine
    component: server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client-re # Explicitly set to your NFS storage class
